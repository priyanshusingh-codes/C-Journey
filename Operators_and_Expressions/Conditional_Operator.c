/*
It is also called the ternary operator (because it uses 3 parts).
The operator pair is "?,:"

Syntax:

exp1 ? exp2 : exp3

example :

a = 10;
b = 15;
x = (a > b) ? a : b;

//It can be also used using if - else statement

if (a > b)
x = a;

else 
x = b;
*/