#include <stdio.h>

#define LENGTH 10   // length of rectangle
#define WIDTH 5     // width of rectangle

int main() {
    int area;

    area = LENGTH * WIDTH;   // calculation using constants

    printf("Area of rectangle = %d\n", area);

    return 0;
}